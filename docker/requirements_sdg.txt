# requirements_sdg.txt – additional dependencies for the SDG container
# (Core SDG Hub dependencies are installed directly in the Dockerfile)

# HTTP / async
httpx>=0.27.0
aiohttp>=3.9.0

# Data processing
pandas>=2.0.0
pyarrow>=14.0.0

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0
